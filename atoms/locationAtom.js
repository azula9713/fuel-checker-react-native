import { atom } from "recoil";

export const districtValueAtom = atom({
  key: "districtValueAtom",
  default: null,
});

export const provinceValueAtom = atom({
  key: "provinceValueAtom",
  default: null,
});

export const cityValueAtom = atom({
  key: "cityValueAtom",
  default: null,
});
